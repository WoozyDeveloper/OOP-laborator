#pragma once
#include "Student.h"

inline bool between(float grade);

int nameComparison(Student s1, Student s2);//compare the names

int mathGradeComparison(Student s1, Student s2);//compare the math grade

int englishGradeComparison(Student s1, Student s2);//compare the english grade

int historyGradeComparison(Student s1, Student s2);//compare the history grade

int averageComparison(Student s1, Student s2);//calculate the average grade